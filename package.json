{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "test": "lerna run test --stream",
    "devfrontend": "lerna run dev --scope=frontend --stream",
    "buildfrontend": "lerna run build --scope=frontend --stream",
    "startfrontend": "lerna run start --scope=frontend --stream",
    "testfrontend": "lerna run test --scope=frontend --stream",
    "devbackend": "lerna run dev --scope=backend --stream",
    "buildbackend": "lerna run build --scope=backend --stream",
    "startbackend": "lerna run start --scope=backend --stream",
    "testbackend": "lerna run test:e2e --scope=backend --stream",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "@tailwindcss/custom-forms": "^0.2.1",
    "@types/jest": "^26.0.20",
    "@types/mongoose": "^5.10.3",
    "lerna": "^4.0.0",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "@types/yup": "^0.29.11",
    "antd": "^4.15.1",
    "date-fns": "^2.19.0",
    "formik": "^2.2.6",
    "mongodb-memory-server": "^6.9.3",
    "mongoose": "^5.10.18",
    "react-loading-skeleton": "^2.2.0",
    "react-nice-dates": "^3.1.0",
    "react-router-dom": "^5.2.0",
    "react-toastify": "^7.0.3",
    "yup": "^0.32.9"
  }
}
