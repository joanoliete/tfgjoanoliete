/** Application errors */
export enum Errors {
	INVALID_OBJECTID = 'ObjectID invalid format',
	INVALID_URL_FORMAT = 'URL invalid format',
	FLIGHT_ALREADY_IN_FAVOURITES = 'UserID have already this flight added to favourites',
}
